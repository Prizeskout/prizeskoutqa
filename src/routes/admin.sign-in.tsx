import{createFileRoute}from"@tanstack/react-router";import{useState,type FormEvent}from"react";import{ShieldCheck,MailCheck}from"lucide-react";import{supabase}from"@/integrations/supabase/client";
export const Route=createFileRoute("/admin/sign-in")({head:()=>({meta:[{title:"Admin sign in | PrizeSkout"}]}),component:Page});
function Page(){const[email,setEmail]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false),[sent,setSent]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const{error:authError}=await supabase.auth.signInWithOtp({email:email.trim().toLowerCase(),options:{shouldCreateUser:false,emailRedirectTo:`${location.origin}/admin/callback`}});setBusy(false);if(authError){setError('Could not send the sign-in email. Check the address and try again.');return}setSent(true)};return <main style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#111827',fontFamily:'Inter,system-ui,sans-serif',padding:20}}><form onSubmit={submit} style={{width:'min(390px,100%)',background:'#fff',borderRadius:16,padding:28,boxShadow:'0 24px 80px #0006'}}><div style={{width:44,height:44,borderRadius:12,display:'grid',placeItems:'center',background:'#FFF1E8',color:'#EA580C'}}>{sent?<MailCheck size={22}/>:<ShieldCheck size={22}/>}</div><h1 style={{fontSize:22,margin:'18px 0 5px'}}>PrizeSkout Admin</h1>{sent?<><p style={{fontSize:13,color:'#4B5563',lineHeight:1.6}}>We sent a secure sign-in link to <b>{email}</b>. Open the email and click the link to access the admin dashboard.</p><button type="button" onClick={()=>setSent(false)} style={{border:0,background:'transparent',padding:0,color:'#EA580C',fontWeight:700,cursor:'pointer'}}>Use another email</button></>:<><p style={{fontSize:13,color:'#6B7280',margin:'0 0 22px'}}>Enter your administrator email. No password is required.</p><label style={{fontSize:12,fontWeight:700}}>Email address<input autoFocus type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} style={field}/></label>{error&&<div role="alert" style={{padding:10,borderRadius:8,background:'#FEF2F2',color:'#B91C1C',fontSize:12,marginBottom:12}}>{error}</div>}<button disabled={busy} type="submit" style={{width:'100%',padding:11,border:0,borderRadius:9,background:'#EA580C',color:'#fff',fontWeight:800,cursor:busy?'wait':'pointer'}}>{busy?'Sending…':'Email me a secure sign-in link'}</button></>}</form></main>};const field:React.CSSProperties={display:'block',width:'100%',boxSizing:'border-box',margin:'6px 0 15px',padding:'10px 11px',border:'1px solid #D1D5DB',borderRadius:8,fontSize:14,outline:'none'};
