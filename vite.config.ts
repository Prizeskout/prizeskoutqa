// Vite + TanStack Start config for Cloudflare Pages deployment.
// @lovable.dev/vite-tanstack-config automatically includes
// @cloudflare/vite-plugin when cloudflare is not explicitly disabled,
// producing a Cloudflare Workers-compatible server bundle alongside
// the static client assets in dist/client/.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig();
